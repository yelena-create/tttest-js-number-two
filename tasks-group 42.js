/** реализовать калькулятор через свитч и кто захочет через if else. Будем запрашивать у пользователя 2 числа, не забывать преобразовывать их и выбирать действие, прибавление, отнимание, умножение, деление, деление по модулю.  результат можно выводить целым примером с помощью шаблонной строки.  */



/** Обработать массив,
const arr = [“BEST”, “the”, “foo”, “is”,  “JS” ]  развернуть массив,
вырезать foo, перевести его в строку разделенную пробелами. */



/**codewars
 * Create a function sumTwoSmallestNumbers(numbers) that returns the sum of the two lowest positive numbers given an array of minimum 4 positive integers. No floats or non-positive integers will be passed.
For example, when an array is passed like [19, 5, 42, 2, 77], the output should be 7.
 */


/**Codewars: Given a list lst and a number N, create a new list that contains each number of lst at most N times without reordering. For example if N = 2, and the input is [1,2,3,1,2,1,2,3], you take [1,2,3,1,2], drop the next [1,2] since this would lead to 1 and 2 being in the result 3 times, and then take 3, which leads to [1,2,3,1,2,3].  Пример вызова функции deleteNth([1,1,3,3,7,2,2,2,2], 3) // return [1, 1, 3, 3, 7, 2, 2, 2] */


